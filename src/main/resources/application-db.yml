spring:
  datasource:
    url: jdbc:postgresql://localhost:5437/user-db?currentSchema=liquibase
    username: mahammad
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate